收到，换用户和部署按你们说的做。

一、换用户
小程序 web-view 先打开：
https://wx-bak.xdp8.cn/get/openid/from/gzh?callbackUrl=我们活动页
回调后取 query.wxOpenid。我们用 wxOpenid 在 H5 内做登录鉴权（交给我们服务端换我们自己的 token，问答抽奖不再把 openid 放请求里）。第一期只用 wxOpenid 当用户载体，不调你们会员信息接口。领奖姓名电话地址我们自己采集。结果页如需跳会员中心/公众号，请给链接。

请再补这几项，我们好开工：
1. callbackUrl 要配我们哪个域名，谁加白名单，现在测试能不能配
2. wxOpenid 有没有签名或时效（抽奖要防 URL 改 openid）
3. 小程序 web-view 的 src 是否由你们写成上述授权地址；wx-bak.xdp8.cn 和我们活动域名是否都会加业务域名
4. 测试环境：授权链接现在能否用，回调到我们测试域名行不行
5. wxOpenid 我们落库做答题/抽奖记录，这一点请书面确认

二、部署
按你们说的：到时给新机器，域名你们提前解析。请同步：
1. 机器系统，能否装 PHP、MySQL、Redis、Nginx（我们现有 ThinkPHP + Vue3）
2. SSH 或堡垒机权限何时给
3. HTTPS 证书谁装，业务域名校验文件谁放到站点根目录
4. 活动域名预计是什么，方便我们先按这个配回调
